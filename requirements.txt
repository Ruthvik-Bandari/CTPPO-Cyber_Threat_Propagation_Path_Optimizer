# Core API
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6
aiofiles>=23.0.0

# ML/AI - CPU only for deployment
--extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers>=4.35.0
anthropic>=0.40.0  # LLM-based code security review (scanners/llm_code_review.py)
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
networkx>=3.0
tqdm>=4.65.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Security & Auth
pyotp>=2.9.0
qrcode>=7.4.0
pillow>=10.0.0
email-validator>=2.1.0
redis>=5.0.0  # B1 server-side session store (api/session_store.py); falls back to in-memory if unset

# Reports
reportlab>=4.0.0

# HTTP
requests>=2.31.0
certifi>=2024.2.2  # CA bundle for EPSS/KEV downloads (core/threat_data.py SSL context)
aiohttp>=3.9.0
PyJWT>=2.8.0

# Database (prod persistence; stores fall back to in-memory if unconfigured)
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
